"""AIgnition probabilistic revenue forecasting utility.

Modules use flat imports (``from config import ...``) so the entry-point scripts
run cleanly via ``python src/predict.py`` (which puts ``src/`` on ``sys.path``)
exactly as the submission contract's example ``run.sh`` invokes them.
"""

__version__ = "1.0.0"
